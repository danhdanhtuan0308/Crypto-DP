[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.airflow"

[deploy]
startCommand = "bash -c 'airflow db init && airflow users create --username admin --password admin --firstname Admin --lastname User --role Admin --email admin@example.com || true && airflow webserver & airflow scheduler'"
healthcheckPath = "/health"
healthcheckTimeout = 300
